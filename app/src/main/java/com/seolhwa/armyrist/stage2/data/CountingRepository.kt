package com.seolhwa.armyrist.stage2.data

/*
 * Intentionally empty compatibility stub.
 *
 * Canonical production CountingRepository:
 *   com.seolhwa.armyrist.data.CountingRepository
 *
 * This path previously contained a byte-for-byte duplicate with the same
 * package/class declaration, which caused Kotlin "Redeclaration" in CI.
 * It can be deleted in a later cleanup release.
 */
